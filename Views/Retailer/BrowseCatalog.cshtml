@model IEnumerable<SCM_System.Models.Entities.Product>

@{
    ViewData["Title"] = "Supplier Catalog";
    Layout = "_DashboardLayout";
    var categories = ViewData["Categories"] as IEnumerable<SCM_System.Models.Entities.ProductCategory>;
}

<div class="container-fluid py-4">
    <!-- Header Section -->
    <div class="glass-card mb-4 p-4 border-0">
        <div class="row align-items-center">
            <div class="col-md-6">
                <h3 class="fw-bold mb-1 text-primary">Supplier Catalog</h3>
                <p class="text-muted mb-0">Browse and discover products from our verified suppliers.</p>
            </div>
            <div class="col-md-6 text-md-end mt-3 mt-md-0">
                <form asp-action="BrowseCatalog" method="get" class="d-flex gap-2">
                    <div class="input-group">
                        <span class="input-group-text bg-white border-end-0 rounded-start-pill ps-3">
                            <i class="fas fa-search text-muted"></i>
                        </span>
                        <input type="text" name="searchTerm" class="form-control border-start-0 ps-0" placeholder="Search products..." value="@Context.Request.Query["searchTerm"]" />
                    </div>
                    <select name="categoryId" class="form-select w-auto" onchange="this.form.submit()">
                        <option value="">All Categories</option>
                        @if (categories != null)
                        {
                            foreach (var cat in categories)
                            {
                                <option value="@cat.Id" selected="@(Context.Request.Query["categoryId"] == cat.Id.ToString())">@cat.CategoryName</option>
                            }
                        }
                    </select>
                    <button type="submit" class="btn btn-primary rounded-pill px-4">Search</button>
                </form>
            </div>
        </div>
    </div>

    <!-- Product Grid -->
    <div class="row row-cols-1 row-cols-md-3 row-cols-lg-4 g-4">
        @if (!Model.Any())
        {
            <div class="col-12">
                <div class="glass-card p-5 text-center">
                    <div class="text-muted opacity-50 mb-3">
                        <i class="fas fa-box-open fa-4x"></i>
                    </div>
                    <h5 class="fw-bold">No Products Found</h5>
                    <p class="text-muted">Try adjusting your filters or search terms.</p>
                    <a asp-action="BrowseCatalog" class="btn btn-outline-primary rounded-pill px-4">Clear All Filters</a>
                </div>
            </div>
        }
        else
        {
            foreach (var product in Model)
            {
                <div class="col">
                    <div class="glass-card h-100 border-0 shadow-sm product-card">
                        <div class="position-relative overflow-hidden" style="height: 200px; border-radius: 12px 12px 0 0;">
                            @if (!string.IsNullOrEmpty(product.ImageUrl))
                            {
                                <img src="@product.ImageUrl" class="w-100 h-100" style="object-fit: cover;" alt="@product.ProductName">
                            }
                            else
                            {
                                <div class="w-100 h-100 d-flex align-items-center justify-content-center bg-light text-muted">
                                    <i class="fas fa-image fa-3x opacity-25"></i>
                                </div>
                            }
                            <div class="position-absolute top-0 end-0 p-2">
                                <span class="badge bg-primary rounded-pill">@product.Category?.CategoryName</span>
                            </div>
                        </div>
                        <div class="p-3">
                            <h6 class="fw-bold mb-1 text-truncate" title="@product.ProductName">@product.ProductName</h6>
                            <div class="d-flex align-items-baseline mb-2">
                                <span class="h5 fw-bold text-primary mb-0">@product.BasePrice.ToString("C")</span>
                                <span class="text-muted small ms-2">/ @product.Unit</span>
                            </div>
                            
                            <div class="mb-3">
                                <div class="small text-muted mb-1">
                                    <i class="fas fa-store me-1"></i> @product.Supplier?.CompanyName
                                </div>
                                @if (product.Quantity < 10)
                                {
                                    <div class="small text-danger fw-bold">
                                        <i class="fas fa-exclamation-triangle me-1"></i> Low Stock: @product.Quantity
                                    </div>
                                }
                                else
                                {
                                    <div class="small text-success">
                                        <i class="fas fa-check-circle me-1"></i> In Stock: @product.Quantity
                                    </div>
                                }
                            </div>
                            
                            <div class="d-grid gap-2">
                                <a asp-action="ProductDetails" asp-route-id="@product.Id" class="btn btn-outline-primary btn-sm rounded-pill">View Details</a>
                                @* Placeholder for Add to Cart or Order *@
                                @* <button class="btn btn-primary btn-sm rounded-pill">Add to Cart</button> *@
                            </div>
                        </div>
                    </div>
                </div>
            }
        }
    </div>
</div>

<style>
    .product-card {
        transition: transform 0.2s, box-shadow 0.2s;
    }
    .product-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 20px rgba(0,0,0,0.1) !important;
    }
    .text-truncate-2 {
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;  
        overflow: hidden;
    }
    .input-group-text {
        border-right: none;
    }
    .form-control:focus {
        box-shadow: none;
        border-color: #dee2e6;
    }
</style>
