@model IEnumerable<SCM_System.Models.Entities.PurchaseOrder>
@{
    ViewData["Title"] = "My Purchase Orders";
    Layout = "_DashboardLayout";
}

<div class="container-fluid py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold text-primary"><i class="fas fa-file-invoice me-2"></i>My Purchase Orders</h2>
        <a asp-controller="Marketplace" asp-action="Index" class="btn btn-primary rounded-pill px-4">
            <i class="fas fa-shopping-cart me-2"></i> Browse Marketplace
        </a>
    </div>

    <div class="glass-card overflow-hidden">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="bg-light text-dark">
                    <tr>
                        <th class="ps-4">PO Number</th>
                        <th>Supplier</th>
                        <th>Total Amount</th>
                        <th>Status</th>
                        <th>Order Date</th>
                        <th class="text-end pe-4">Actions</th>
                    </tr>
                </thead>
                <tbody>
                @foreach(var po in Model) {
                    <tr>
                        <td class="ps-4 fw-bold text-primary">@po.PONumber</td>
                        <td>
                            <div class="fw-bold">@po.Supplier?.CompanyName</div>
                            <small class="text-muted">ID: #@po.SupplierId</small>
                        </td>
                        <td class="fw-bold">@po.TotalAmount.ToString("C")</td>
                        <td>
                            <span class="badge rounded-pill p-2 px-3 @(po.Status == "Pending" ? "bg-warning text-dark" : (po.Status == "Accepted" ? "bg-success" : "bg-danger"))">
                                @po.Status
                            </span>
                        </td>
                        <td>@po.OrderDate.ToString("MMM dd, yyyy")</td>
                        <td class="text-end pe-4">
                            <a asp-action="Details" asp-route-id="@po.Id" class="btn btn-sm btn-outline-primary rounded-pill px-3">
                                <i class="fas fa-info-circle me-1"></i> Details
                            </a>
                        </td>
                    </tr>
                }
                @if(!Model.Any()) {
                    <tr>
                        <td colspan="6" class="text-center py-5 text-muted">
                            <i class="fas fa-file-invoice fa-3x mb-3 d-block opacity-25"></i>
                            No purchase orders found.
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    </div>
</div>
